function valid_dept_name(name) {
    if(name.length==0 || name.length >16) return false;
    return true;
}

function valid_dept_code(code){
    if(code.length==0 || code.length >16) return false;
    return true;
}

